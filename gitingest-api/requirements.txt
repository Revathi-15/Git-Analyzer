# Web framework
fastapi>=0.112.0
uvicorn>=0.30.0

# GitHub repo ingestion
gitingest>=0.3.1

# HTTP clients
httpx>=0.27.0
aiohttp>=3.9.1

# Data validation
pydantic>=2.8.0

# Environment variables
python-dotenv>=1.0.0

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
# HuggingFace embeddings (sentence-transformers model runs locally, no API key)
sentence-transformers>=3.0.0

# FAISS — local vector store for similarity search
faiss-cpu>=1.8.0

# Text splitting / chunking
langchain-text-splitters>=0.2.0

# NumPy (required by FAISS + sentence-transformers)
numpy>=1.26.0
